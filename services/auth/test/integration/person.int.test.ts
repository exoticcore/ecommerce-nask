import supertest from 'supertest';
import { describe, it, expect, jest } from '@jest/globals';

describe('Person API Integration Testing', () => {
  it.todo('create check email endpoint /active-email');
  it.todo('create login with email endpoint /login');
});
